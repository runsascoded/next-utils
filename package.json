{
  "name": "next-utils",
  "version": "0.1.2",
  "type": "module",
  "workspaces": ["packages/base", "packages/plotly"],
  "scripts": {
    "build": "tsc && npm run cp",
    "clean": "rm -rf dist",
    "cleanbuild": "npm run clean && npm run build",
    "cp": "cp package.json README.md next-utils*.sh write-redirects.js dist/",
    "dev": "tsc -w"
  }
}
