{
  "name": "duckdb-utils",
  "version": "0.1.2",
  "type": "module",
  "scripts": {
    "build": "tsc && npm run cp",
    "clean": "rm -rf dist",
    "cleanbuild": "npm run clean && npm run build",
    "cp": "cp package.json dist/",
    "dev": "tsc -w"
  },
  "dependencies": {
    "@duckdb/duckdb-wasm": "^1.28.0",
    "web-worker": "^1.3.0"
  },
  "devDependencies": {
    "@types/emscripten": "^1.39.6",
    "typescript": "^4.9.3"
  }
}
